cache/
out/
node_modules/
.wrangler/
.claude/
# Anchored to repo root only — the root package.json/lock are dev-only (foundry
# helpers). server/package.json MUST stay tracked so Render can build the API.
/package.json
/package-lock.json
.env
